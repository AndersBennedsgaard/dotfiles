---
- name: Ensure group "{{ user_group }}" exists
  group:
    name: "{{ user_group }}"
    state: present

- name: Ensure the user "{{ user_name }}" belongs to "{{ user_group }}"
  user:
    name: "{{ user_name }}"
    shell: /bin/bash
    group: "{{ user_group }}"
